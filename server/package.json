{
  "name": "hypebound-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HYPEBOUND authoritative match server — Cloudflare Workers + Durable Objects. Imports ../src/engine verbatim; never forks it.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
