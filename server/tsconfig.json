{
  "//": [
    "Deliberately compiles the shared engine a SECOND time, under a different lib.",
    "",
    "`include` reaches back into ../src/engine and ../src/net, and `lib` has no DOM in",
    "it. That is not an accident and it is not redundant with the root typecheck: it is",
    "the compile-time proof that the engine the server imports verbatim (multiplayer",
    "§15, sequencing rule 1) does not quietly depend on a browser. The day somebody adds",
    "a `document.` or a `window.` to a rules file, this fails — in the repo, not on",
    "workerd at 2 a.m.",
    "",
    "Every other option matches the root tsconfig on purpose. Two strictness settings",
    "for one body of code would mean the engine has two meanings."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "types": ["@cloudflare/workers-types"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "useDefineForClassFields": true
  },
  "//include": [
    "`../src/engine` is listed in full because the claim being checked is that the",
    "*whole* engine is portable, not merely the parts the server happens to import",
    "today. A file nobody imports yet is exactly where a `document.` survives.",
    "",
    "`../src/net` is deliberately NOT listed. It is only partly shared: `protocol.ts`,",
    "`view.ts` and `transport.ts` are, and the compiler reaches them anyway by",
    "following the imports in `src/shared/wire.ts`. `wsTransport.ts` and",
    "`localTransport.ts` are client files — the first drives a browser `WebSocket`,",
    "whose event-handler properties workerd's WebSocket does not have, and the second",
    "imports the match driver and the AI. Blanket-including the directory compiled",
    "both of them against the wrong platform and failed for a reason that was not a",
    "bug in either."
  ],
  "include": ["src", "../src/engine", "../data"]
}
